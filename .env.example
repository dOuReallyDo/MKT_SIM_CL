# Credenziali per le API
ALPHA_VANTAGE_API_KEY=YOUR_ALPHA_VANTAGE_API_KEY_HERE

# Configurazione proxy (opzionale)
PROXY_URL=http://your-proxy-url:port

# Chiave di crittografia (generare con python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# Configurazione Redis (opzionale)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Path di logging
LOG_PATH=./logs 